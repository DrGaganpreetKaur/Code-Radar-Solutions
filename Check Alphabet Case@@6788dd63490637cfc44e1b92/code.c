#include <stdio.h>

int main() {
    char ch;
    if(isupper(ch))

    printf("Uppercase");
    else
    if(islower(ch))
    prntf("Lowercase");
    else
    printf("Not an alphabet");
    return 0;
}